{
  "name": "back",
  "version": "1.0.0",
  "author": "Ana Luiza",
  "description": "",
  "main": "src/server.ts",
  "private": true,
  "license": "private",
  "scripts": {
    "build": "tsc",
    "start": "ts-node-dev --transpile-only --unhandled-rejections=strict src/server.ts",
    "start:dev": "ts-node-dev --transpile-only --ignore-watch node_modules src/server.ts",
    "typeorm": "ts-node-dev ./node_modules/typeorm/cli.js",
    "clean": "rm -rf ./node_modules && rm -rf ./package-lock.json && npm cache clean --force",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@types/cors": "^2.8.8",
    "@types/express": "^4.17.8",
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "errors": "^0.3.0",
    "express": "^4.17.1",
    "express-async": "^0.1.3",
    "express-async-errors": "^3.1.1",
    "sqlite3": "^5.0.0",
    "ts-node-dev": "^1.0.0",
    "typeorm": "^0.2.29",
    "typescript": "^4.0.5",
    "yup": "^0.30.0"
  },
  "devDependencies": {
    "@types/yup": "^0.29.9"
  }
}
